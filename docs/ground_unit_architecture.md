# 客户端（Ground Unit）实现方案

## 1. 系统定位

客户端是独立的Python应用，负责：

- 通过mDNS发现机载端
- 接收图传数据（UDP）
- 解码与渲染视频
- 接收用户输入（键盘/鼠标/手柄）
- 发送控制指令到机载端
- 发送参数修改请求
- 显示实时状态（FPS、延迟、丢包率）

**关键原则**：UI与网络完全分离，网络层独立线程处理，UI线程不阻塞。

---

## 2. 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│ 客户端应用（Python）                                         │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ UI 层                                                │  │
│  │ - 视频渲染（OpenGL/QML）                             │  │
│  │ - 控制输入（键盘/鼠标/手柄）                         │  │
│  │ - 参数面板                                           │  │
│  │ - 状态显示（FPS、延迟、丢包率）                      │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ 业务逻辑层                                           │  │
│  │ - 会话管理（连接/断开/重连）                         │  │
│  │ - 输入映射（WASD → 控制指令）                        │  │
│  │ - 参数管理（查询/修改）                              │  │
│  │ - 状态监听                                           │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
└────────────────┬──────────────────────────────────────────┘
                 │
┌────────────────▼──────────────────────────────────────────┐
│ 网络层（独立线程）                                          │
├────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ mDNS 线程                                            │  │
│  │ - 查询服务 → 获取IP/端口 → 回调业务层                │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ RX 线程（图传接收）                                   │  │
│  │ - UDP接收 → FEC解码 → 重组 → 解码 → 渲染队列        │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ TX 线程（控制发送）                                   │  │
│  │ - 命令队列 → 打包 → 发送 → 等待ACK                   │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ 心跳线程                                             │  │
│  │ - 定期发送心跳 → 连接状态检测                        │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
└────────────────┬──────────────────────────────────────────┘
                 │
        ┌────────▼────────┐
        │   UDP Socket    │
        │   mDNS Socket   │
        └────────┬────────┘
                 │
        ┌────────▼────────┐
        │   局域网        │
        └─────────────────┘
```

---

## 3. 模块设计

### 3.1 UI 层

**职责**：
- 显示视频流
- 处理用户输入
- 显示状态信息
- 参数面板

**主要组件**：

| 组件 | 职责 |
|------|------|
| VideoRenderer | 渲染视频帧（OpenGL） |
| ControlPanel | 参数修改面板 |
| StatusBar | 显示FPS、延迟、丢包率 |
| InputHandler | 捕获键盘/鼠标/手柄输入 |

#### 3.1.1 前端技术栈选择

**推荐方案：Pygame + PyImGui**

采用Pygame + PyImGui的原因：
- **Pygame**：游戏引擎，主循环、输入、渲染都优化过，天然适合FPS风格应用
- **PyImGui**：Dear ImGui的Python绑定，专为实时应用设计，轻量级UI库，不阻塞主循环

**架构**：
```
┌─────────────────────────────────────┐
│ Pygame（主循环 + 渲染）              │
├─────────────────────────────────────┤
│                                     │
│  ┌─────────────────────────────┐   │
│  │ 主画面（OpenGL纹理）         │   │
│  │ 显示图传                     │   │
│  └─────────────────────────────┘   │
│                                     │
│  ┌─────────────────────────────┐   │
│  │ PyImGui（UI层）              │   │
│  │ - 右下角状态栏               │   │
│  │ - ESC菜单                    │   │
│  │ - 参数面板                   │   │
│  └─────────────────────────────┘   │
│                                     │
└─────────────────────────────────────┘
```

**技术栈对比**：

| 方案 | 优点 | 缺点 | 推荐度 |
|------|------|------|--------|
| **Pygame + PyImGui** | 轻量、游戏导向、UI库成熟、帧率60+ | 需要学习ImGui | ⭐⭐⭐⭐⭐ |
| **Pyglet + PyImGui** | 更现代、更轻量、帧率60+ | 生态小 | ⭐⭐⭐⭐ |
| **PySide6 + QML** | 功能完整、跨平台 | 不够游戏化、性能一般、帧率30-60 | ⭐⭐⭐ |
| 纯 **Pygame** | 最轻量 | UI需自己实现 | ⭐⭐ |

**性能对比**：

| 方案 | 帧率 | 延迟 | CPU占用 |
|------|------|------|--------|
| Pygame + PyImGui | 60+ | <5ms | 低 |
| Pyglet + PyImGui | 60+ | <5ms | 低 |
| PySide6 + QML | 30-60 | 10-20ms | 中 |

**依赖安装**：
```bash
pip install pygame imgui[pygame] numpy opencv-python zeroconf
```

#### 3.1.2 UI布局设计

**主界面**：
```
┌─────────────────────────────────────┐
│                                     │
│                                     │
│      [视频画面 - 全屏]              │
│                                     │
│                                     │
│                                     │
│                    ┌──────────────┐ │
│                    │ FPS: 30      │ │
│                    │ Ping: 45ms   │ │
│                    │ Loss: 0.2%   │ │
│                    └──────────────┘ │
└─────────────────────────────────────┘
```

**ESC菜单（覆盖层）**：
```
┌─────────────────────────────────────┐
│  [连接] [参数] [关于] [退出]         │
├─────────────────────────────────────┤
│ 服务名: air_unit_01                 │
│ 状态: 已连接                        │
│ 分辨率: 1920x1080                   │
│ 码率: 5000 kbps                     │
└─────────────────────────────────────┘
```

#### 3.1.3 主循环实现

```python
class GroundUnit:
    def __init__(self):
        self.window = pygame.display.set_mode((1920, 1080))
        self.render_queue = RenderQueue()
        self.session = SessionManager(self.render_queue)
        self.show_menu = False
        self.fps_clock = pygame.time.Clock()

    def run(self):
        # 启动网络线程
        self.session.start()

        running = True
        while running:
            # 1. 输入处理
            for event in pygame.event.get():
                if event.type == pygame.QUIT:
                    running = False
                elif event.type == pygame.KEYDOWN:
                    if event.key == pygame.K_ESCAPE:
                        self.show_menu = not self.show_menu
                    self.handle_input(event)

            # 2. 更新网络（从RenderQueue读帧）
            frame = self.render_queue.get_latest()

            # 3. 渲染
            glClear(GL_COLOR_BUFFER_BIT)
            self.render_video_texture(frame)  # 全屏视频

            # 4. ImGui UI
            imgui.new_frame()

            if self.show_menu:
                self.draw_menu()  # ESC菜单
            else:
                self.draw_status_bar()  # 右下角状态

            imgui.render()

            # 5. 交换缓冲
            pygame.display.flip()

            # 6. 帧率控制
            self.fps_clock.tick(60)

        self.session.stop()
```

#### 3.1.4 目录结构

```
ground_unit/
├─ main.py                 # 入口
├─ ui/
│  ├─ renderer.py         # 视频渲染（OpenGL纹理）
│  ├─ imgui_ui.py         # ImGui UI组件（菜单、状态栏、参数面板）
│  ├─ theme.py            # UI主题（暗色/游戏风格）
│  └─ input_handler.py    # 输入处理（键盘/鼠标/手柄）
├─ network/
│  ├─ video_receiver.py   # RX线程
│  ├─ control_sender.py   # TX线程
│  ├─ service_discovery.py # mDNS发现
│  └─ session.py          # 会话管理
├─ logic/
│  ├─ input_mapper.py     # 输入映射（WASD → 控制指令）
│  ├─ param_manager.py    # 参数管理
│  └─ status_monitor.py   # 状态监听
└─ config.py              # 配置
```

### 3.2 业务逻辑层

**职责**：
- 管理连接生命周期
- 映射用户输入到控制指令
- 管理参数查询/修改
- 监听机载端状态

**主要组件**：

| 组件 | 职责 |
|------|------|
| SessionManager | 连接/断开/重连状态机 |
| InputMapper | WASD/鼠标 → 控制指令 |
| ParamManager | 参数查询/修改/缓存 |
| StatusMonitor | FPS/延迟/丢包率统计 |

**会话状态机**：
```
┌─────────┐
│ Idle    │
└────┬────┘
     │ 用户点击连接
     ▼
┌─────────────┐
│ Discovering │ (mDNS查询)
└────┬────────┘
     │ 发现成功
     ▼
┌──────────────┐
│ Connecting   │ (建立UDP连接)
└────┬─────────┘
     │ 连接成功
     ▼
┌──────────────┐
│ Connected    │ (正常工作)
└────┬─────────┘
     │ 超时/用户断开
     ▼
┌──────────────┐
│ Disconnected │
└────┬─────────┘
     │ 自动重连或用户重试
     ▼
┌─────────────────┐
│ Reconnecting    │
└────┬────────────┘
     │ 重连成功/失败
     ▼
┌──────────────┐
│ Connected    │ 或 Idle
└──────────────┘
```

### 3.3 ServiceDiscovery（mDNS线程）

**职责**：
- 查询mDNS服务
- 获取机载端IP、端口
- 回调给SessionManager

**工作流程**：
```
1. 用户输入mDNS服务名（如"air_unit_01"）
2. mDNS线程查询
3. 获取IP、video_port、control_port
4. 回调SessionManager
5. SessionManager建立UDP连接
```

**使用库**：`zeroconf` (Python)

### 3.4 VideoReceiver（RX线程）

**职责**：
- UDP接收分片包
- 按FrameID分类
- FEC解码恢复丢失片段
- 重组完整帧
- 解码（FFmpeg）
- 放入渲染队列

**数据流**：
```
UDP接收 → PacketBuffer → FECDecoder → Reassembler → Decoder → RenderQueue → UI
```

**关键组件**：

| 组件 | 职责 |
|------|------|
| UDPReceiver | UDP接收 |
| PacketBuffer | 按FrameID缓冲分片 |
| FECDecoder | Reed-Solomon解码 |
| Reassembler | 按PacketID重组 |
| Decoder | FFmpeg解码 |
| RenderQueue | 渲染队列（最多缓冲3帧） |

**消息格式**（与机载端一致）：
```
┌─────────────┬──────────┬──────────┬──────────┬──────────┬─────────┐
│ FrameHeader │ FrameID  │ PacketID │ Total    │ Payload  │ CRC32   │
│ (4 bytes)   │ (4 bytes)│ (2 bytes)│ (2 bytes)│ (var)    │ (4 bytes)│
└─────────────┴──────────┴──────────┴──────────┴──────────┴─────────┘
```

**ACK机制**：
- 接收完整帧后发送ACK
- 格式：`[Magic][Version][MsgType=0x05][FrameID][CRC32]`

### 3.5 ControlSender（TX线程）

**职责**：
- 从命令队列读取控制指令
- 打包消息
- 计算CRC
- UDP发送
- 等待ACK（超时重传）

**数据流**：
```
InputMapper → CommandQueue → MessageBuilder → UDPSender → 机载端
                                                    ↓
                                              等待ACK（100ms超时）
                                                    ↓
                                              超时则重传（最多3次）
```

**消息格式**（与机载端一致）：
```
┌──────────┬─────────┬──────────┬──────────┬──────────┬─────────┐
│ Magic    │ Version │ MsgType  │ Seq      │ Payload  │ CRC32   │
│ (2 bytes)│ (1 byte)│ (1 byte) │ (4 bytes)│ (var)    │ (4 bytes)│
└──────────┴─────────┴──────────┴──────────┴──────────┴─────────┘
```

**消息类型**：
- 0x01: 控制指令（ControlCommand）
- 0x02: 参数修改（ParamUpdate）
- 0x03: 参数查询（ParamQuery）
- 0x04: 心跳（Heartbeat）
- 0x05: ACK（Acknowledgement）

### 3.6 HeartbeatManager（心跳线程）

**职责**：
- 定期发送心跳包
- 监测连接状态
- 超时检测与重连

**工作流程**：
```
1. 每100ms发送一个心跳包
2. 记录发送时间
3. 如果100ms内未收到任何数据，标记为超时
4. 连续超时3次，触发重连
```

---

## 4. 线程模型

```
主线程（UI事件循环）
├─ 渲染视频帧
├─ 处理用户输入
└─ 更新状态显示

mDNS线程（ServiceDiscovery）
├─ 查询mDNS服务
└─ 回调SessionManager

RX线程（VideoReceiver）
├─ UDP接收
├─ FEC解码
├─ 重组
├─ 解码
└─ 放入RenderQueue

TX线程（ControlSender）
├─ 读取CommandQueue
├─ 打包消息
├─ UDP发送
└─ 等待ACK

心跳线程（HeartbeatManager）
├─ 定期发送心跳
└─ 连接状态检测
```

**线程安全**：
- RenderQueue：`queue.Queue` (线程安全)
- CommandQueue：`queue.Queue` (线程安全)
- 状态变量：`threading.Lock` + `threading.Condition`
- 原子操作：`threading.Event` 用于停止信号

---

## 5. 输入映射

### 5.1 键盘映射（FPS风格）

| 按键 | 功能 | 控制指令 |
|------|------|---------|
| W | 前进 | forward=1.0 |
| S | 后退 | forward=-1.0 |
| A | 左转 | turn=-1.0 |
| D | 右转 | turn=1.0 |
| Space | 跳跃/动作 | action=1 |
| Shift | 冲刺 | sprint=1.0 |
| ESC | 菜单 | 显示/隐藏菜单 |

### 5.2 鼠标映射（FPS风格）

| 操作 | 功能 | 控制指令 |
|------|------|---------|
| 鼠标移动 | 视角旋转 | pitch/yaw |
| 左键 | 射击/动作 | fire=1 |
| 右键 | 瞄准 | aim=1 |
| 滚轮 | 切换武器 | weapon_select |

### 5.3 手柄映射（可选）

| 按键/轴 | 功能 | 控制指令 |
|---------|------|---------|
| 左摇杆 | 移动 | forward/turn |
| 右摇杆 | 视角 | pitch/yaw |
| A/X | 动作 | action |
| LT/RT | 射击 | fire |

---

## 6. 配置文件

```yaml
# config.yaml
client:
  # UI配置
  ui:
    render_width: 1920
    render_height: 1080
    target_fps: 30
    fullscreen: false
    
  # 网络配置
  network:
    mdns_service: "air_unit_01"      # 要连接的mDNS服务名
    auto_connect: true               # 自动连接
    reconnect_interval: 5            # 重连间隔(秒)
    heartbeat_interval: 100          # 心跳间隔(ms)
    ack_timeout: 100                 # ACK超时(ms)
    max_retries: 3                   # 最大重试次数
    
  # 输入配置
  input:
    control_mode: "fps"              # fps/tank/custom
    mouse_sensitivity: 1.0
    invert_pitch: false
    
  # 调试配置
  debug:
    show_stats: true                 # 显示统计信息
    show_fps: true                   # 显示FPS
    show_latency: true               # 显示延迟
    show_packet_loss: true           # 显示丢包率
    log_level: "INFO"                # DEBUG/INFO/WARNING/ERROR
```

---

## 7. 状态显示

### 7.1 实时统计

```
┌─────────────────────────────────────┐
│ FPS: 29.8  Latency: 45ms  Loss: 0.2% │
│ Bitrate: 4.2Mbps  Frames: 1234      │
└─────────────────────────────────────┘
```

**统计项**：
- **FPS**：渲染帧率（每秒更新）
- **Latency**：往返延迟（RTT）
- **Loss**：丢包率（%）
- **Bitrate**：实时码率（Mbps）
- **Frames**：已接收帧数

### 7.2 连接状态

```
连接状态：
  ○ Idle (灰色)
  ◐ Discovering (黄色)
  ◑ Connecting (黄色)
  ● Connected (绿色)
  ◕ Disconnected (红色)
  ◔ Reconnecting (黄色)
```

---

## 8. 错误处理与恢复

### 8.1 网络故障

| 故障 | 检测方式 | 恢复策略 |
|------|---------|---------|
| 连接超时 | 心跳无响应 | 自动重连 |
| 丢包过高 | 丢包率>50% | 降低码率/分辨率 |
| 解码失败 | 解码异常 | 跳过帧，继续接收 |
| 缓冲溢出 | RenderQueue满 | 丢弃最旧帧 |

### 8.2 降级策略

```
正常状态（丢包<5%）
  ↓
轻度丢包（5%-20%）→ 降低帧率（30→15fps）
  ↓
中度丢包（20%-50%）→ 降低分辨率（1080p→720p）
  ↓
严重丢包（>50%）→ 降低码率（5Mbps→2Mbps）
  ↓
连接中断 → 自动重连
```

---

## 9. 性能指标

### 9.1 目标指标

| 指标 | 目标值 |
|------|--------|
| 端到端延迟 | <100ms |
| 帧率 | 30fps |
| 分辨率 | 1920x1080 |
| 码率 | 5Mbps |
| 丢包率 | <1% |
| CPU占用 | <30% |
| 内存占用 | <200MB |

### 9.2 监测方式

#### 9.2.1 高精度延迟测量（四时间戳方案）

**原理**：使用客户端和机载端的四个时间戳，计算 RTT、时钟偏移、单向延迟。

**流程**：
```
步骤 1：客户端 → 机载端
  t1 = 客户端发送时间

步骤 2：机载端 → 客户端
  t2 = 机载端收到时间
  t3 = 机载端发出 ACK 时间

步骤 3：客户端收到 ACK
  t4 = 客户端收到时间
```

**计算公式**：
```
RTT = t4 - t1

Clock Offset（客户端相对机载端的时钟偏移）
offset = ((t2 - t1) + (t3 - t4)) / 2

单向延迟（客户端 → 机载端）
delay_up = (t2 - t1) - offset

单向延迟（机载端 → 客户端）
delay_down = (t4 - t3) - offset
```

**实现**：
```python
# 客户端发送
t1 = time.perf_counter()
seq = latency_calc.record_send(t1)
message = Protocol.build_control_command(seq, t1, forward=1.0, ...)
send_udp(message)

# 机载端接收并回复
t2 = time.perf_counter()
msg_type, seq, t1_recv, payload = Protocol.parse_message(data)
# 处理控制指令
t3 = time.perf_counter()
ack = Protocol.build_ack(seq, t2, t3)
send_udp(ack)

# 客户端接收 ACK
t4 = time.perf_counter()
msg_type, seq, t2_recv, t3_recv = Protocol.parse_ack(ack_data)
rtt, offset, delay_up, delay_down = latency_calc.record_ack(seq, t2_recv, t3_recv, t4)
```

**优势**：
- ✓ 自动补偿时钟偏移
- ✓ 分别测量上行/下行延迟
- ✓ 检测非对称网络
- ✓ 精度 ±5%

#### 9.2.2 其他统计

```python
# FPS统计
frame_count = 0
last_time = time.perf_counter()
# ... 每渲染一帧
frame_count += 1
if time.perf_counter() - last_time >= 1.0:
    fps = frame_count
    frame_count = 0
    last_time = time.perf_counter()

# 丢包率统计
expected_frames = total_frames_received + lost_frames
packet_loss = lost_frames / expected_frames * 100
```

---

## 10. 与机载端的协议交互

### 10.1 连接建立

```
客户端                          机载端
  │                              │
  ├─ mDNS查询 ──────────────────→ │
  │                              │
  │ ←─────── mDNS响应 ──────────┤
  │ (IP, video_port, control_port)
  │                              │
  ├─ 心跳包(t1) ──────────────→ │
  │                              │
  │ ←─ ACK(t2, t3) ────────────┤
  │                              │
  └─ 连接建立 ─────────────────→ │
```

### 10.2 图传流程

```
机载端                          客户端
  │                              │
  ├─ 分片包1 ──────────────────→ │
  │                              │
  ├─ 分片包2 ──────────────────→ │
  │                              │
  ├─ 分片包3 ──────────────────→ │
  │                              │
  │ ←─────── ACK ──────────────┤
  │                              │
  └─ 下一帧 ──────────────────→ │
```

### 10.3 控制流程（含延迟测量）

**四时间戳延迟测量**：

```
客户端                          机载端
  │                              │
  ├─ 控制指令(seq, t1) ────────→ │
  │                              │
  │                    t2 = 收到时间
  │                    处理指令
  │                    t3 = 发出时间
  │                              │
  │ ←─ ACK(seq, t2, t3) ────────┤
  │                              │
  t4 = 收到时间
  计算：
    RTT = t4 - t1
    offset = ((t2-t1) + (t3-t4)) / 2
    delay_up = (t2-t1) - offset
    delay_down = (t4-t3) - offset
  │                              │
  └─ 继续发送 ──────────────────→ │
```

**消息格式**：

控制指令：
```
┌──────────┬─────────┬──────────┬──────────┬──────────┬──────────┬──────────┬─────────┐
│ Magic    │ Version │ MsgType  │ Reserved │ Seq      │ t1       │ Payload  │ CRC32   │
│ (2 bytes)│ (1 byte)│ (1 byte) │ (1 byte) │ (4 bytes)│ (8 bytes)│ (var)    │ (4 bytes)│
└──────────┴─────────┴──────────┴──────────┴──────────┴──────────┴──────────┴─────────┘
```

ACK 回复：
```
┌──────────┬─────────┬──────────┬──────────┬──────────┬──────────┬──────────┬─────────┐
│ Magic    │ Version │ MsgType  │ Reserved │ Seq      │ t2       │ t3       │ CRC32   │
│ (2 bytes)│ (1 byte)│ (1 byte) │ (1 byte) │ (4 bytes)│ (8 bytes)│ (8 bytes)│ (4 bytes)│
└──────────┴─────────┴──────────┴──────────┴──────────┴──────────┴──────────┴─────────┘
```

---

## 10.4 高精度延迟计算（四时间戳方案）

**原理**：使用客户端和机载端的四个时间戳，精确计算 RTT、时钟偏移、单向延迟。

**流程**：
```
步骤 1：客户端发送
  t1 = time.perf_counter()（客户端发送时间）
  seq = 序列号
  发送消息：[Magic][Version][MsgType][Seq][t1][Payload][CRC32]

步骤 2：机载端接收并回复
  t2 = time.perf_counter()（机载端接收时间）
  处理控制指令
  t3 = time.perf_counter()（机载端发出时间）
  发送 ACK：[Magic][Version][MsgType][Seq][t2][t3][CRC32]

步骤 3：客户端接收
  t4 = time.perf_counter()（客户端接收时间）
  计算延迟指标
```

**计算公式**：
```
RTT（往返延迟）
  RTT = t4 - t1

Clock Offset（时钟偏移）
  offset = ((t2 - t1) + (t3 - t4)) / 2

单向延迟（上行：客户端→机载端）
  delay_up = (t2 - t1) - offset

单向延迟（下行：机载端→客户端）
  delay_down = (t4 - t3) - offset
```

**消息格式**：

控制指令（客户端→机载端）：
```
┌──────────┬─────────┬──────────┬──────────┬──────────┬──────────┬──────────┬─────────┐
│ Magic    │ Version │ MsgType  │ Reserved │ Seq      │ t1       │ Payload  │ CRC32   │
│ (2 bytes)│ (1 byte)│ (1 byte) │ (1 byte) │ (4 bytes)│ (8 bytes)│ (var)    │ (4 bytes)│
└──────────┴─────────┴──────────┴──────────┴──────────┴──────────┴──────────┴─────────┘
```

ACK 回复（机载端→客户端）：
```
┌──────────┬─────────┬──────────┬──────────┬──────────┬──────────┬──────────┬─────────┐
│ Magic    │ Version │ MsgType  │ Reserved │ Seq      │ t2       │ t3       │ CRC32   │
│ (2 bytes)│ (1 byte)│ (1 byte) │ (1 byte) │ (4 bytes)│ (8 bytes)│ (8 bytes)│ (4 bytes)│
└──────────┴─────────┴──────────┴──────────┴──────────┴──────────┴──────────┴─────────┘
```

**实现**：
```python
# 客户端发送
t1 = time.perf_counter()
seq = latency_calc.record_send(t1)
message = Protocol.build_control_command(seq, t1, forward=1.0, ...)
send_udp(message)

# 机载端接收并回复
t2 = time.perf_counter()
msg_type, seq, t1_recv, payload = Protocol.parse_message(data)
# 处理控制指令
t3 = time.perf_counter()
ack = Protocol.build_ack(seq, t2, t3)
send_udp(ack)

# 客户端接收 ACK
t4 = time.perf_counter()
msg_type, seq, t2_recv, t3_recv = Protocol.parse_ack(ack_data)
result = latency_calc.record_ack(seq, t2_recv, t3_recv, t4)

# 获取结果
print(f"RTT: {result['rtt']:.1f}ms")
print(f"Up: {result['delay_up']:.1f}ms, Down: {result['delay_down']:.1f}ms")
print(f"Offset: {result['offset']:.1f}ms")
```

**优势**：
- ✓ 自动补偿时钟偏移
- ✓ 分别测量上行/下行延迟
- ✓ 检测非对称网络
- ✓ 使用 `perf_counter()` 微秒级精度
- ✓ 精度 ±5%（vs 简单 RTT 的 ±50%）

**关键实现点**：
1. 使用 `time.perf_counter()` 而非 `time.time()`
2. t2 应在接收 UDP 包时立即记录
3. t3 应在发送 ACK 前记录
4. ACK 必须包含相同的 seq，以便客户端匹配
5. 实现异常值过滤（3 倍标准差规则）
6. 实现超时检测（清理 5 秒未回复的包）

---

## 11. 依赖库

| 库 | 用途 | 版本 |
|----|------|------|
| pygame | 游戏引擎 | >=2.0 |
| imgui | UI库 | >=1.4 |
| FFmpeg-python | 视频解码 | >=0.2 |
| zeroconf | mDNS发现 | >=0.40 |
| numpy | 数据处理 | >=1.20 |
| opencv-python | 图像处理 | >=4.5 |
| pygame-input | 手柄支持 | >=0.1 |

---

## 12. 扩展功能规划

### 12.1 高优先级功能（Phase 2）

#### 12.1.1 录制与回放系统

**功能**：
- 录制视频流（H.264编码保存）
- 录制控制指令序列（带时间戳）
- 录制网络统计数据
- 回放功能（可调速）
- 截图功能

**UI设计**：
```
ESC菜单 → 录制选项卡
┌─ 录制 ─────────────────────┐
│ [●] 开始录制               │
│ 录制时长: 00:05:32         │
│ 文件大小: 125 MB           │
│ [⏹] 停止录制               │
│ [📁] 打开录制文件夹         │
└────────────────────────────┘
```

**文件格式**：
```
录制文件结构：
  video.mp4          # H.264编码视频
  commands.log       # 控制指令序列
  stats.json         # 网络统计数据
  metadata.json      # 元数据（时间、机器人、参数等）
```

**价值**：
- 调试网络问题时可回放
- 性能分析
- 事故复现

---

#### 12.1.2 网络诊断面板

**功能**：
- 实时带宽使用（上行/下行）
- 丢包率详情（丢包数、重传数）
- RTT分布（最小/平均/最大）
- 编码器统计（编码时间、关键帧间隔）
- 解码器统计（解码时间、帧缓冲）
- 网络包统计（收发包数、错误包数）

**UI设计**：
```
ESC菜单 → 诊断选项卡
┌─ 网络诊断 ─────────────────────┐
│ 上行带宽: 0.5 Mbps             │
│ 下行带宽: 5.2 Mbps             │
│ 丢包数: 12 / 10000             │
│ 重传数: 3                      │
│ RTT: 45ms (min: 40, max: 120)  │
│ 编码时间: 15ms                 │
│ 解码时间: 8ms                  │
│ 缓冲帧数: 2                    │
│ [导出报告]                     │
└────────────────────────────────┘
```

**数据来源**：
- 机载端发送的统计包
- 客户端本地测量
- 实时图表展示

**价值**：
- 快速定位网络问题
- 性能瓶颈分析
- 网络优化决策

---

#### 12.1.3 自定义按键映射

**功能**：
- 自定义方向键（WASD）
- 自定义视角控制（鼠标）
- 自定义快捷键
- 自定义手柄按键
- 保存/加载预设

**UI设计**：
```
ESC菜单 → 控制设置 → 按键映射
┌─ 控制映射 ─────────────────────┐
│ 前进: [W]                      │
│ 后退: [S]                      │
│ 左转: [A]                      │
│ 右转: [D]                      │
│ 加速: [Shift]                  │
│ 特殊功能1: [E]                 │
│ 特殊功能2: [F]                 │
│ [重置为默认] [保存预设]         │
└────────────────────────────────┘
```

**配置文件**：
```json
{
  "presets": {
    "default": {
      "forward": "w",
      "backward": "s",
      "left": "a",
      "right": "d",
      "sprint": "shift"
    },
    "wasd_alt": {
      "forward": "up",
      "backward": "down",
      "left": "left",
      "right": "right"
    }
  }
}
```

**价值**：
- 适应不同用户习惯
- 支持多种控制方式

---

#### 12.1.4 手柄支持

**功能**：
- 支持Xbox 360/One/Series
- 支持PlayStation 4/5
- 支持通用HID手柄
- 摇杆映射（左摇杆移动、右摇杆视角）
- 按键映射（A/B/X/Y等）
- 扳机映射（LT/RT）
- 死区设置
- 振动反馈

**UI设计**：
```
ESC菜单 → 控制设置 → 手柄配置
┌─ 手柄配置 ─────────────────────┐
│ 已连接: Xbox One 手柄          │
│ 左摇杆: 移动                   │
│ 右摇杆: 视角                   │
│ A按键: 跳跃                    │
│ B按键: 蹲下                    │
│ 死区: [████░░░░░░] 15%         │
│ 振动: [启用]                   │
└────────────────────────────────┘
```

**价值**：
- 提升控制体验
- 支持更多用户

---

#### 12.1.5 多机器人支持

**功能**：
- 同时连接多个机器人
- 分屏显示（2x2、1+3等布局）
- 快速切换（快捷键或点击）
- 独立控制每个机器人
- 同步控制（一个指令发给多个机器人）

**UI设计**：
```
主画面支持分屏
┌─────────────────┬─────────────────┐
│  air_unit_01    │  air_unit_02    │
│                 │                 │
│  [视频1]        │  [视频2]        │
│                 │                 │
├─────────────────┼─────────────────┤
│  air_unit_03    │  air_unit_04    │
│                 │                 │
│  [视频3]        │  [视频4]        │
│                 │                 │
└─────────────────┴─────────────────┘

快捷键：1/2/3/4 切换焦点机器人
```

**布局选项**：
- 1x1（单机器人）
- 2x1（两个机器人）
- 1x2（两个机器人）
- 2x2（四个机器人）
- 1+3（一个大 + 三个小）

**价值**：
- 支持多机器人协作
- 提高工作效率

---

### 12.2 中优先级功能（Phase 3）

#### 12.2.1 自动码率调整

**功能**：
- 根据丢包率自动调整码率
- 根据延迟自动调整帧率
- 根据带宽充足逐步提升质量
- 网络恢复后自动恢复

**调整策略**：
```
正常状态（丢包<5%）
  ↓
轻度丢包（5%-20%）→ 降低帧率（30→15fps）
  ↓
中度丢包（20%-50%）→ 降低分辨率（1080p→720p）
  ↓
严重丢包（>50%）→ 降低码率（5Mbps→2Mbps）
  ↓
连接中断 → 自动重连
```

**UI设计**：
```
ESC菜单 → 图传参数 → 自适应设置
┌─ 自适应设置 ─────────────────┐
│ [✓] 启用自动码率调整          │
│ 最小码率: 2000 kbps           │
│ 最大码率: 10000 kbps          │
│ 丢包阈值: 5%                  │
│ 延迟阈值: 200ms               │
│ 调整间隔: 5秒                 │
└───────────────────────────────┘
```

**价值**：
- 自动适应网络变化
- 提高稳定性

---

#### 12.2.2 操作日志与审计

**功能**：
- 记录连接/断开时间
- 记录控制指令（时间、操作）
- 记录参数修改（时间、修改内容）
- 记录异常事件（断连、高延迟等）
- 导出日志报告

**UI设计**：
```
ESC菜单 → 日志选项卡
┌─ 操作日志 ─────────────────────┐
│ 2026-04-18 10:30:45 连接成功   │
│ 2026-04-18 10:31:02 启用控制   │
│ 2026-04-18 10:31:15 发送前进   │
│ 2026-04-18 10:32:00 修改码率   │
│ 2026-04-18 10:35:30 网络丢包   │
│ [导出日志] [清空日志]           │
└────────────────────────────────┘
```

**日志格式**：
```
[2026-04-18 10:30:45] [INFO] 连接成功: air_unit_01 (192.168.1.23)
[2026-04-18 10:31:02] [INFO] 启用控制模式
[2026-04-18 10:31:15] [CONTROL] 前进 (value: 1.0)
[2026-04-18 10:32:00] [PARAM] 修改码率: 5000 → 3000 kbps
[2026-04-18 10:35:30] [WARNING] 丢包率过高: 8.5%
```

**价值**：
- 安全审计
- 问题追踪
- 性能分析

---

#### 12.2.3 性能监控面板

**功能**：
- 显示客户端CPU占用
- 显示客户端内存占用
- 显示GPU占用（如果用GPU解码）
- 显示解码时间
- 显示渲染时间
- 显示帧缓冲大小
- 实时图表

**UI设计**：
```
ESC菜单 → 性能选项卡
┌─ 性能监控 ─────────────────────┐
│ CPU: [████░░░░░░] 35%          │
│ 内存: [███░░░░░░░] 28%         │
│ GPU: [██░░░░░░░░] 15%          │
│ 解码时间: 8ms                  │
│ 渲染时间: 5ms                  │
│ 帧缓冲: 2 / 3                  │
│ [实时图表]                     │
└────────────────────────────────┘
```

**图表类型**：
- CPU占用趋势（过去60秒）
- 内存占用趋势
- 解码时间分布
- 帧率变化

**价值**：
- 性能优化
- 资源管理

---

#### 12.2.4 快捷键系统

**功能**：
- 预定义快捷键
- 自定义快捷键
- 快捷键冲突检测
- 快捷键预设保存

**预定义快捷键**：
```
Ctrl+C: 快速连接上次机器人
Ctrl+D: 快速断开
Ctrl+R: 快速录制
Ctrl+S: 截图
Ctrl+P: 暂停/恢复
1-4: 切换多机器人焦点
Tab: 显示/隐藏UI
ESC: 打开/关闭菜单
```

**UI设计**：
```
ESC菜单 → 快捷键选项卡
┌─ 快捷键设置 ─────────────────┐
│ 快速连接: [Ctrl+C]            │
│ 快速断开: [Ctrl+D]            │
│ 开始录制: [Ctrl+R]            │
│ 截图: [Ctrl+S]               │
│ 暂停: [Ctrl+P]                │
│ 显示UI: [Tab]                 │
│ [恢复默认]                    │
└───────────────────────────────┘
```

**价值**：
- 提高操作效率
- 支持高级用户

---

#### 12.2.5 场景管理

**功能**：
- 保存连接配置（机器人、IP、端口）
- 保存显示设置（分辨率、全屏等）
- 保存控制配置（按键映射）
- 保存图传参数（码率、帧率等）
- 快速加载场景

**UI设计**：
```
ESC菜单 → 场景选项卡
┌─ 场景管理 ─────────────────────┐
│ 当前场景: [默认 ▼]             │
│ ☐ 室内场景                    │
│ ☐ 室外场景                    │
│ ☐ 高延迟网络                  │
│ ☐ 低带宽网络                  │
│ [新建场景] [保存当前] [删除]   │
└────────────────────────────────┘
```

**场景配置文件**：
```json
{
  "scenes": {
    "default": {
      "resolution": "1920x1080",
      "fullscreen": true,
      "bitrate": 5000,
      "fps": 30
    },
    "low_bandwidth": {
      "resolution": "1280x720",
      "fullscreen": false,
      "bitrate": 2000,
      "fps": 15
    }
  }
}
```

**价值**：
- 快速切换配置
- 适应不同场景

---

### 12.3 低优先级功能（Phase 4）

#### 12.3.1 画面增强
- 对比度/亮度调整
- 降噪处理
- 锐化处理
- 色彩校正

#### 12.3.2 虚拟操纵杆
- 屏幕上显示虚拟摇杆（触屏支持）
- 虚拟按钮
- 自定义布局

#### 12.3.3 实时通信
- 文字聊天（与机器人操作者）
- 语音通话
- 消息历史

#### 12.3.4 网络诊断工具
- Ping测试
- 带宽测试
- 丢包测试
- 网络质量评分

---

## 13. 开发流程

### 12.1 Phase 1：基础框架（1周）
- [ ] UI框架搭建
- [ ] mDNS发现实现
- [ ] UDP收发基础
- [ ] 简单视频显示

### 12.2 Phase 2：核心功能（2周）
- [ ] 完整图传链路
- [ ] 控制指令发送
- [ ] 参数修改
- [ ] 状态显示

### 12.3 Phase 3：优化与完善（1周）
- [ ] FEC解码
- [ ] 自适应码率
- [ ] 错误恢复
- [ ] 性能优化

---

## 13. 测试清单

- [ ] mDNS发现正常
- [ ] UDP连接建立
- [ ] 图传延迟<100ms
- [ ] 控制指令响应<50ms
- [ ] 丢包恢复正常
- [ ] 断网自动重连
- [ ] UI不卡顿
- [ ] 内存无泄漏
